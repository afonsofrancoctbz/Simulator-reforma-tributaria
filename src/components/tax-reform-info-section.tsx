"use client";

/**
 * Esta seção foi desabilitada para limpar a interface do simulador.
 * O componente retorna 'null', o que faz com que o React não renderize nada no DOM,
 * mas mantém a exportação válida para não quebrar importações em outros arquivos.
 */
export default function TaxReformInfoSection() {
  return null;
}